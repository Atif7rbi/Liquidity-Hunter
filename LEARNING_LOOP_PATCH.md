# Learning Loop Patch Included

This packaged project already includes:
- TradeOutcome table
- Outcome logger after trade close
- Setup × Regime performance analyzer
- Adaptive weights + threshold overrides
- learning section in config.yaml

Run after extract:

```bash
python -m src.main init
bash run.sh
```
